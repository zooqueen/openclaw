---
title: "openclaw/plugin-sdk/webhook-ingress"
summary: "Webhook request validation, target registration, and routing helpers."
---

# `openclaw/plugin-sdk/webhook-ingress`

Webhook request validation, target registration, and routing helpers.

> This page is auto-generated from the TypeScript interface. This module is
> currently classified as `unstable`.

## Import

```ts
import { ... } from "openclaw/plugin-sdk/webhook-ingress";
```

## Module facts

- Category: `channel`
- Stability: `unstable`
- Module source: [src/plugin-sdk/webhook-ingress.ts](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-ingress.ts#L1)

## Export index

| Export                                                                                  | Kind       | Summary                                                                                           |
| --------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------- |
| [`applyBasicWebhookRequestGuards`](#applybasicwebhookrequestguards)                     | `function` | Apply method, rate-limit, and content-type guards before a webhook handler reads the body.        |
| [`beginWebhookRequestPipelineOrReject`](#beginwebhookrequestpipelineorreject)           | `function` | Start the shared webhook request lifecycle and return a release hook for in-flight tracking.      |
| [`createBoundedCounter`](#createboundedcounter)                                         | `function` | Count keyed events in memory with optional TTL pruning and bounded cardinality.                   |
| [`createFixedWindowRateLimiter`](#createfixedwindowratelimiter)                         | `function` | Create a simple fixed-window rate limiter for in-memory webhook protection.                       |
| [`createWebhookAnomalyTracker`](#createwebhookanomalytracker)                           | `function` | Track repeated webhook failures and emit sampled logs for suspicious request patterns.            |
| [`createWebhookInFlightLimiter`](#createwebhookinflightlimiter)                         | `function` | Create an in-memory limiter that caps concurrent webhook handlers per key.                        |
| [`isJsonContentType`](#isjsoncontenttype)                                               | `function` | Detect JSON content types, including structured syntax suffixes like `application/ld+json`.       |
| [`isRequestBodyLimitError`](#isrequestbodylimiterror)                                   | `function` | No inline TSDoc yet.                                                                              |
| [`normalizeWebhookPath`](#normalizewebhookpath)                                         | `function` | Normalize webhook paths into the canonical registry form used by route lookup.                    |
| [`readJsonWebhookBodyOrReject`](#readjsonwebhookbodyorreject)                           | `function` | Read and parse a JSON webhook body, rejecting malformed or oversized payloads consistently.       |
| [`readRequestBodyWithLimit`](#readrequestbodywithlimit)                                 | `function` | No inline TSDoc yet.                                                                              |
| [`readWebhookBodyOrReject`](#readwebhookbodyorreject)                                   | `function` | Read a webhook request body with bounded size/time limits and translate failures into responses.  |
| [`registerPluginHttpRoute`](#registerpluginhttproute)                                   | `function` | No inline TSDoc yet.                                                                              |
| [`registerWebhookTarget`](#registerwebhooktarget)                                       | `function` | Add a normalized target to a path bucket and clean up route state when the last target leaves.    |
| [`registerWebhookTargetWithPluginRoute`](#registerwebhooktargetwithpluginroute)         | `function` | Register a webhook target and lazily install the matching plugin HTTP route on first use.         |
| [`requestBodyErrorToText`](#requestbodyerrortotext)                                     | `function` | No inline TSDoc yet.                                                                              |
| [`resolveSingleWebhookTarget`](#resolvesinglewebhooktarget)                             | `function` | Match exactly one synchronous target or report whether resolution was empty or ambiguous.         |
| [`resolveSingleWebhookTargetAsync`](#resolvesinglewebhooktargetasync)                   | `function` | Async variant of single-target resolution for auth checks that need I/O.                          |
| [`resolveWebhookPath`](#resolvewebhookpath)                                             | `function` | Resolve the effective webhook path from explicit path, URL, or default fallback.                  |
| [`resolveWebhookTargets`](#resolvewebhooktargets)                                       | `function` | Resolve all registered webhook targets for the incoming request path.                             |
| [`resolveWebhookTargetWithAuthOrReject`](#resolvewebhooktargetwithauthorreject)         | `function` | Resolve an authorized target and send the standard unauthorized or ambiguous response on failure. |
| [`resolveWebhookTargetWithAuthOrRejectSync`](#resolvewebhooktargetwithauthorrejectsync) | `function` | Synchronous variant of webhook auth resolution for cheap in-memory match checks.                  |
| [`withResolvedWebhookRequestPipeline`](#withresolvedwebhookrequestpipeline)             | `function` | Run common webhook guards, then dispatch only when the request path resolves to live targets.     |
| [`WEBHOOK_ANOMALY_COUNTER_DEFAULTS`](#webhook_anomaly_counter_defaults)                 | `const`    | No inline TSDoc yet.                                                                              |
| [`WEBHOOK_ANOMALY_STATUS_CODES`](#webhook_anomaly_status_codes)                         | `const`    | No inline TSDoc yet.                                                                              |
| [`WEBHOOK_BODY_READ_DEFAULTS`](#webhook_body_read_defaults)                             | `const`    | No inline TSDoc yet.                                                                              |
| [`WEBHOOK_IN_FLIGHT_DEFAULTS`](#webhook_in_flight_defaults)                             | `const`    | No inline TSDoc yet.                                                                              |
| [`WEBHOOK_RATE_LIMIT_DEFAULTS`](#webhook_rate_limit_defaults)                           | `const`    | No inline TSDoc yet.                                                                              |
| [`BoundedCounter`](#boundedcounter)                                                     | `type`     | No inline TSDoc yet.                                                                              |
| [`FixedWindowRateLimiter`](#fixedwindowratelimiter)                                     | `type`     | No inline TSDoc yet.                                                                              |
| [`RegisteredWebhookTarget`](#registeredwebhooktarget)                                   | `type`     | No inline TSDoc yet.                                                                              |
| [`RegisterWebhookPluginRouteOptions`](#registerwebhookpluginrouteoptions)               | `type`     | No inline TSDoc yet.                                                                              |
| [`RegisterWebhookTargetOptions`](#registerwebhooktargetoptions)                         | `type`     | No inline TSDoc yet.                                                                              |
| [`WebhookAnomalyTracker`](#webhookanomalytracker)                                       | `type`     | No inline TSDoc yet.                                                                              |
| [`WebhookBodyReadProfile`](#webhookbodyreadprofile)                                     | `type`     | No inline TSDoc yet.                                                                              |
| [`WebhookInFlightLimiter`](#webhookinflightlimiter)                                     | `type`     | No inline TSDoc yet.                                                                              |
| [`WebhookTargetMatchResult`](#webhooktargetmatchresult)                                 | `type`     | No inline TSDoc yet.                                                                              |

## API

### applyBasicWebhookRequestGuards

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:148](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L148)

Apply method, rate-limit, and content-type guards before a webhook handler reads the body.

**Declaration**

```ts
export function applyBasicWebhookRequestGuards(params: {
  req: IncomingMessage;
  res: ServerResponse<IncomingMessage>;
  allowMethods?: readonly string[] | undefined;
  rateLimiter?: FixedWindowRateLimiter | undefined;
  rateLimitKey?: string | undefined;
  nowMs?: number | undefined;
  requireJsonContentType?: boolean | undefined;
}): boolean;
```

### beginWebhookRequestPipelineOrReject

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:189](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L189)

Start the shared webhook request lifecycle and return a release hook for in-flight tracking.

**Declaration**

```ts
export function beginWebhookRequestPipelineOrReject(params: { req: IncomingMessage; res: ServerResponse<IncomingMessage>; allowMethods?: readonly string[] | undefined; rateLimiter?: FixedWindowRateLimiter | undefined; ... 6 more ...; inFlightLimitMessage?: string | undefined; }): { ...; } | { ...; };
```

### createBoundedCounter

- Kind: `function`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:109](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L109)

Count keyed events in memory with optional TTL pruning and bounded cardinality.

**Declaration**

```ts
export function createBoundedCounter(options: {
  maxTrackedKeys: number;
  ttlMs?: number | undefined;
  pruneIntervalMs?: number | undefined;
}): BoundedCounter;
```

### createFixedWindowRateLimiter

- Kind: `function`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:52](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L52)

Create a simple fixed-window rate limiter for in-memory webhook protection.

**Declaration**

```ts
export function createFixedWindowRateLimiter(options: {
  windowMs: number;
  maxRequests: number;
  maxTrackedKeys: number;
  pruneIntervalMs?: number | undefined;
}): FixedWindowRateLimiter;
```

### createWebhookAnomalyTracker

- Kind: `function`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:167](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L167)

Track repeated webhook failures and emit sampled logs for suspicious request patterns.

**Declaration**

```ts
export function createWebhookAnomalyTracker(
  options?:
    | {
        maxTrackedKeys?: number | undefined;
        ttlMs?: number | undefined;
        logEvery?: number | undefined;
        trackedStatusCodes?: readonly number[] | undefined;
      }
    | undefined,
): WebhookAnomalyTracker;
```

### createWebhookInFlightLimiter

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:91](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L91)

Create an in-memory limiter that caps concurrent webhook handlers per key.

**Declaration**

```ts
export function createWebhookInFlightLimiter(
  options?:
    | { maxInFlightPerKey?: number | undefined; maxTrackedKeys?: number | undefined }
    | undefined,
): WebhookInFlightLimiter;
```

### isJsonContentType

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:138](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L138)

Detect JSON content types, including structured syntax suffixes like `application/ld+json`.

**Declaration**

```ts
export function isJsonContentType(value: string | string[] | undefined): boolean;
```

### isRequestBodyLimitError

- Kind: `function`
- Source: [src/infra/http-body.ts:46](https://github.com/openclaw/openclaw/blob/main/src/infra/http-body.ts#L46)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export function isRequestBodyLimitError(
  error: unknown,
  code?: RequestBodyLimitErrorCode | undefined,
): error is RequestBodyLimitError;
```

### normalizeWebhookPath

- Kind: `function`
- Source: [src/plugin-sdk/webhook-path.ts:2](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-path.ts#L2)

Normalize webhook paths into the canonical registry form used by route lookup.

**Declaration**

```ts
export function normalizeWebhookPath(raw: string): string;
```

### readJsonWebhookBodyOrReject

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:275](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L275)

Read and parse a JSON webhook body, rejecting malformed or oversized payloads consistently.

**Declaration**

```ts
export function readJsonWebhookBodyOrReject(params: { req: IncomingMessage; res: ServerResponse<IncomingMessage>; maxBytes?: number | undefined; timeoutMs?: number | undefined; profile?: WebhookBodyReadProfile | undefined; emptyObjectOnEmpty?: boolean | undefined; invalidJsonMessage?: string | undefined; }): Promise<...>;
```

### readRequestBodyWithLimit

- Kind: `function`
- Source: [src/infra/http-body.ts:121](https://github.com/openclaw/openclaw/blob/main/src/infra/http-body.ts#L121)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export function readRequestBodyWithLimit(
  req: IncomingMessage,
  options: ReadRequestBodyOptions,
): Promise<string>;
```

### readWebhookBodyOrReject

- Kind: `function`
- Source: [src/plugin-sdk/webhook-request-guards.ts:240](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L240)

Read a webhook request body with bounded size/time limits and translate failures into responses.

**Declaration**

```ts
export function readWebhookBodyOrReject(params: { req: IncomingMessage; res: ServerResponse<IncomingMessage>; maxBytes?: number | undefined; timeoutMs?: number | undefined; profile?: WebhookBodyReadProfile | undefined; invalidBodyMessage?: string | undefined; }): Promise<...>;
```

### registerPluginHttpRoute

- Kind: `function`
- Source: [src/plugins/http-registry.ts:12](https://github.com/openclaw/openclaw/blob/main/src/plugins/http-registry.ts#L12)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export function registerPluginHttpRoute(params: { path?: string | null | undefined; fallbackPath?: string | null | undefined; handler: PluginHttpRouteHandler; auth: OpenClawPluginHttpRouteAuth; ... 6 more ...; registry?: PluginRegistry | undefined; }): () => void;
```

### registerWebhookTarget

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:61](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L61)

Add a normalized target to a path bucket and clean up route state when the last target leaves.

**Declaration**

```ts
export function registerWebhookTarget<T extends { path: string }>(
  targetsByPath: Map<string, T[]>,
  target: T,
  opts?: RegisterWebhookTargetOptions<T> | undefined,
): RegisteredWebhookTarget<T>;
```

### registerWebhookTargetWithPluginRoute

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:30](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L30)

Register a webhook target and lazily install the matching plugin HTTP route on first use.

**Declaration**

```ts
export function registerWebhookTargetWithPluginRoute<T extends { path: string; }>(params: { targetsByPath: Map<string, T[]>; target: T; route: RegisterWebhookPluginRouteOptions; onLastPathTargetRemoved?: ((params: { ...; }) => void) | undefined; }): RegisteredWebhookTarget<...>;
```

### requestBodyErrorToText

- Kind: `function`
- Source: [src/infra/http-body.ts:59](https://github.com/openclaw/openclaw/blob/main/src/infra/http-body.ts#L59)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export function requestBodyErrorToText(code: RequestBodyLimitErrorCode): string;
```

### resolveSingleWebhookTarget

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:193](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L193)

Match exactly one synchronous target or report whether resolution was empty or ambiguous.

**Declaration**

```ts
export function resolveSingleWebhookTarget<T>(
  targets: readonly T[],
  isMatch: (target: T) => boolean,
): WebhookTargetMatchResult<T>;
```

### resolveSingleWebhookTargetAsync

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:212](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L212)

Async variant of single-target resolution for auth checks that need I/O.

**Declaration**

```ts
export function resolveSingleWebhookTargetAsync<T>(
  targets: readonly T[],
  isMatch: (target: T) => Promise<boolean>,
): Promise<WebhookTargetMatchResult<T>>;
```

### resolveWebhookPath

- Kind: `function`
- Source: [src/plugin-sdk/webhook-path.ts:15](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-path.ts#L15)

Resolve the effective webhook path from explicit path, URL, or default fallback.

**Declaration**

```ts
export function resolveWebhookPath(params: {
  webhookPath?: string | undefined;
  webhookUrl?: string | undefined;
  defaultPath?: string | null | undefined;
}): string | null;
```

### resolveWebhookTargets

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:107](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L107)

Resolve all registered webhook targets for the incoming request path.

**Declaration**

```ts
export function resolveWebhookTargets<T>(
  req: IncomingMessage,
  targetsByPath: Map<string, T[]>,
): { path: string; targets: T[] } | null;
```

### resolveWebhookTargetWithAuthOrReject

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:231](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L231)

Resolve an authorized target and send the standard unauthorized or ambiguous response on failure.

**Declaration**

```ts
export function resolveWebhookTargetWithAuthOrReject<T>(params: { targets: readonly T[]; res: ServerResponse<IncomingMessage>; isMatch: (target: T) => boolean | Promise<boolean>; unauthorizedStatusCode?: number | undefined; unauthorizedMessage?: string | undefined; ambiguousStatusCode?: number | undefined; ambiguousMessage?: string | undefined; }): Promise<...>;
```

### resolveWebhookTargetWithAuthOrRejectSync

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:247](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L247)

Synchronous variant of webhook auth resolution for cheap in-memory match checks.

**Declaration**

```ts
export function resolveWebhookTargetWithAuthOrRejectSync<T>(params: {
  targets: readonly T[];
  res: ServerResponse<IncomingMessage>;
  isMatch: (target: T) => boolean;
  unauthorizedStatusCode?: number | undefined;
  unauthorizedMessage?: string | undefined;
  ambiguousStatusCode?: number | undefined;
  ambiguousMessage?: string | undefined;
}): T | null;
```

### withResolvedWebhookRequestPipeline

- Kind: `function`
- Source: [src/plugin-sdk/webhook-targets.ts:121](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L121)

Run common webhook guards, then dispatch only when the request path resolves to live targets.

**Declaration**

```ts
export function withResolvedWebhookRequestPipeline<T>(params: { req: IncomingMessage; res: ServerResponse<IncomingMessage>; targetsByPath: Map<string, T[]>; allowMethods?: readonly string[] | undefined; ... 8 more ...; handle: (args: { ...; }) => boolean | ... 1 more ... | Promise<...>; }): Promise<...>;
```

### WEBHOOK_ANOMALY_COUNTER_DEFAULTS

- Kind: `const`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:31](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L31)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export const WEBHOOK_ANOMALY_COUNTER_DEFAULTS: Readonly<{
  maxTrackedKeys: 4096;
  ttlMs: number;
  logEvery: 25;
}>;
```

### WEBHOOK_ANOMALY_STATUS_CODES

- Kind: `const`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:37](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L37)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export const WEBHOOK_ANOMALY_STATUS_CODES: readonly number[];
```

### WEBHOOK_BODY_READ_DEFAULTS

- Kind: `const`
- Source: [src/plugin-sdk/webhook-request-guards.ts:19](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L19)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export const WEBHOOK_BODY_READ_DEFAULTS: Readonly<{
  preAuth: { maxBytes: number; timeoutMs: number };
  postAuth: { maxBytes: number; timeoutMs: number };
}>;
```

### WEBHOOK_IN_FLIGHT_DEFAULTS

- Kind: `const`
- Source: [src/plugin-sdk/webhook-request-guards.ts:30](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L30)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export const WEBHOOK_IN_FLIGHT_DEFAULTS: Readonly<{ maxInFlightPerKey: 8; maxTrackedKeys: 4096 }>;
```

### WEBHOOK_RATE_LIMIT_DEFAULTS

- Kind: `const`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:25](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L25)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export const WEBHOOK_RATE_LIMIT_DEFAULTS: Readonly<{
  windowMs: 60000;
  maxRequests: 120;
  maxTrackedKeys: 4096;
}>;
```

### BoundedCounter

- Kind: `type`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:19](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L19)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type BoundedCounter = BoundedCounter;
```

### FixedWindowRateLimiter

- Kind: `type`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:13](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L13)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type FixedWindowRateLimiter = FixedWindowRateLimiter;
```

### RegisteredWebhookTarget

- Kind: `type`
- Source: [src/plugin-sdk/webhook-targets.ts:10](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L10)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type RegisteredWebhookTarget = RegisteredWebhookTarget<T>;
```

### RegisterWebhookPluginRouteOptions

- Kind: `type`
- Source: [src/plugin-sdk/webhook-targets.ts:24](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L24)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type RegisterWebhookPluginRouteOptions = RegisterWebhookPluginRouteOptions;
```

### RegisterWebhookTargetOptions

- Kind: `type`
- Source: [src/plugin-sdk/webhook-targets.ts:15](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L15)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type RegisterWebhookTargetOptions = RegisterWebhookTargetOptions<T>;
```

### WebhookAnomalyTracker

- Kind: `type`
- Source: [src/plugin-sdk/webhook-memory-guards.ts:39](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-memory-guards.ts#L39)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type WebhookAnomalyTracker = WebhookAnomalyTracker;
```

### WebhookBodyReadProfile

- Kind: `type`
- Source: [src/plugin-sdk/webhook-request-guards.ts:11](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L11)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type WebhookBodyReadProfile = WebhookBodyReadProfile;
```

### WebhookInFlightLimiter

- Kind: `type`
- Source: [src/plugin-sdk/webhook-request-guards.ts:35](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-request-guards.ts#L35)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type WebhookInFlightLimiter = WebhookInFlightLimiter;
```

### WebhookTargetMatchResult

- Kind: `type`
- Source: [src/plugin-sdk/webhook-targets.ts:170](https://github.com/openclaw/openclaw/blob/main/src/plugin-sdk/webhook-targets.ts#L170)

No inline TSDoc is available for this export yet.

**Declaration**

```ts
export type WebhookTargetMatchResult = WebhookTargetMatchResult<T>;
```

## Related modules

- [`openclaw/plugin-sdk/channel-actions`](/reference/plugin-sdk/modules/channel-actions)
- [`openclaw/plugin-sdk/channel-config-schema`](/reference/plugin-sdk/modules/channel-config-schema)
- [`openclaw/plugin-sdk/channel-contract`](/reference/plugin-sdk/modules/channel-contract)
- [`openclaw/plugin-sdk/channel-pairing`](/reference/plugin-sdk/modules/channel-pairing)
- [`openclaw/plugin-sdk/channel-reply-pipeline`](/reference/plugin-sdk/modules/channel-reply-pipeline)
- [`openclaw/plugin-sdk/channel-setup`](/reference/plugin-sdk/modules/channel-setup)
- [`openclaw/plugin-sdk/command-auth`](/reference/plugin-sdk/modules/command-auth)
- [`openclaw/plugin-sdk/secret-input`](/reference/plugin-sdk/modules/secret-input)

## Generated from

- `scripts/generate-plugin-sdk-docs.ts`
- `src/plugin-sdk/webhook-ingress.ts`
