title: Matrix E2EE rejects a recovery key from a different account
scenario:
  id: matrix-e2ee-wrong-account-recovery-key
  surface: channels
  coverage:
    primary:
      - channels.access-and-identity
  objective: "Preserve the retired qa-matrix behavior: Matrix E2EE rejects a recovery key from a
    different account."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-e2ee-wrong-account-recovery-key with the original
      Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix E2EE rejects a recovery key from a different account
    timeoutMs: 180000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        encryption: true
        startupVerification: off
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-wrong-account-recovery-key
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE Wrong Account Key Room
            requireMention: true
