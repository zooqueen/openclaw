title: Matrix E2EE total state loss restores backup with an external recovery key
scenario:
  id: matrix-e2ee-state-loss-external-recovery-key
  surface: channels
  coverage:
    primary:
      - channels.access-and-identity
  objective: "Preserve the retired qa-matrix behavior: Matrix E2EE total state loss restores backup
    with an external recovery key."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-e2ee-state-loss-external-recovery-key with the
      original Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix E2EE total state loss restores backup with an external recovery key
    timeoutMs: 180000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        encryption: true
        startupVerification: off
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-state-loss-external-recovery-key
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE State Loss External Key Room
            requireMention: true
