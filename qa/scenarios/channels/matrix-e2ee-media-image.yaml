title: Matrix E2EE encrypted image attachments reach the model vision path
scenario:
  id: matrix-e2ee-media-image
  surface: channels
  coverage:
    primary:
      - channels.media-rich-content
  objective: "Preserve the retired qa-matrix behavior: Matrix E2EE encrypted image attachments reach
    the model vision path."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-e2ee-media-image with the original Matrix-specific
      assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix E2EE encrypted image attachments reach the model vision path
    timeoutMs: 180000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        encryption: true
        startupVerification: off
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-media-image
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE Media Image Room
            requireMention: true
