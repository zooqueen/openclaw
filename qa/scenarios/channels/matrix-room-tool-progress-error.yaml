title: Matrix streaming finalizes previews after tool errors
scenario:
  id: matrix-room-tool-progress-error
  surface: channels
  coverage:
    primary:
      - matrix.message-presentation-metadata
  execution:
    profiles: { "matrix:all": 12, "matrix:transport": 11 }
    kind: flow
    channel: matrix
    timeoutMs: 60000
    retryCount: 0
    config:
      matrixConfigOverrides:
        streaming: quiet
        toolProfile: coding

flow:
  module: ./live-transports/matrix/scenarios/scenario-runtime-room.js
  call: runToolProgressErrorScenario
  args:
    - expr: "scenarioContext"
