title: Matrix reactions preserve threaded reply targets
scenario:
  id: matrix-reaction-threaded
  surface: channels
  coverage:
    primary:
      - channels.message-actions
  objective: "Preserve the retired qa-matrix behavior: Matrix reactions preserve threaded reply targets."
  successCriteria:
    - The QA Lab flow completes matrix-reaction-threaded through the Matrix adapter with the original Matrix-specific
      assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/matrix-scenario.fixture.ts
  execution:
    kind: flow
    channel: matrix
    summary: Matrix reactions preserve threaded reply targets
    timeoutMs: 45000
    retryCount: 0
    config:
      requiredChannelDriver: live

flow:
  steps:
    - name: Matrix reactions preserve threaded reply targets
      actions:
        - set: matrixFixtures
          value:
            expr: "await qaImport('./matrix-scenario.fixture.js')"
        - call: matrixFixtures.runReactionThreadedScenario
          args:
            - expr: "await transport.fixtureApi.getContext({ requireCanary: true })"
          saveAs: result
      detailsExpr: "result.details ?? (result.artifacts ? JSON.stringify(result.artifacts, null, 2) : undefined)"
