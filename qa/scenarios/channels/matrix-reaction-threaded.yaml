title: Matrix reactions preserve threaded reply targets
scenario:
  id: matrix-reaction-threaded
  surface: channels
  coverage:
    primary:
      - channels.message-actions
  objective: "Preserve the retired qa-matrix behavior: Matrix reactions preserve threaded reply targets."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-reaction-threaded with the original Matrix-specific
      assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix reactions preserve threaded reply targets
    timeoutMs: 45000
    retryCount: 0
    config:
      requiredChannelDriver: live
