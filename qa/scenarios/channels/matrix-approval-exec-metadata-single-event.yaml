title: Matrix exec approval prompt carries structured metadata on one event
scenario:
  id: matrix-approval-exec-metadata-single-event
  surface: channels
  coverage:
    primary:
      - channels.native-approval-prompts
  objective: "Preserve the retired qa-matrix behavior: Matrix exec approval prompt carries structured
    metadata on one event."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-approval-exec-metadata-single-event with the
      original Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix exec approval prompt carries structured metadata on one event
    timeoutMs: 75000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        approvalForwarding:
          exec: true
        dm:
          enabled: true
        execApprovals:
          enabled: true
          target: channel
