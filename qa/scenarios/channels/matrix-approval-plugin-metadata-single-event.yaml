title: Matrix plugin approval prompt carries plugin metadata
scenario:
  id: matrix-approval-plugin-metadata-single-event
  surface: channels
  coverage:
    primary:
      - channels.native-approval-prompts
  execution:
    kind: flow
    channel: matrix
    timeoutMs: 75000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        approvalForwarding:
          plugin: true
        dm:
          enabled: true
        execApprovals:
          enabled: true
          target: channel

flow:
  module: ./live-transports/matrix/scenarios/scenario-runtime-approval.js
  call: runApprovalPluginMetadataSingleEventScenario
  args:
    - expr: "scenarioContext"
