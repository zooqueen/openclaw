title: Matrix E2EE CLI encryption setup targets one account in a multi-account config
scenario:
  id: matrix-e2ee-cli-encryption-setup-multi-account
  surface: channels
  coverage:
    primary:
      - channels.access-and-identity
  objective: "Preserve the retired qa-matrix behavior: Matrix E2EE CLI encryption setup targets one
    account in a multi-account config."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-e2ee-cli-encryption-setup-multi-account with the
      original Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix E2EE CLI encryption setup targets one account in a multi-account config
    timeoutMs: 120000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        encryption: true
        startupVerification: off
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-cli-encryption-setup-multi-account
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE CLI Multi Account Setup Room
            requireMention: true
