title: Matrix reaction removals are observed as redactions
scenario:
  id: matrix-reaction-redaction-observed
  surface: channels
  coverage:
    primary:
      - channels.message-actions
  objective: "Preserve the retired qa-matrix behavior: Matrix reaction removals are observed as redactions."
  successCriteria:
    - The QA Lab flow completes matrix-reaction-redaction-observed through the Matrix adapter with the original
      Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/matrix-scenario.fixture.ts
  execution:
    kind: flow
    channel: matrix
    summary: Matrix reaction removals are observed as redactions
    timeoutMs: 45000
    retryCount: 0
    config:
      requiredChannelDriver: live

flow:
  steps:
    - name: Matrix reaction removals are observed as redactions
      actions:
        - set: matrixFixtures
          value:
            expr: "await qaImport('./matrix-scenario.fixture.js')"
        - call: matrixFixtures.runReactionRedactionObservedScenario
          args:
            - expr: "await transport.fixtureApi.getContext({ requireCanary: true })"
          saveAs: result
      detailsExpr: "result.details ?? (result.artifacts ? JSON.stringify(result.artifacts, null, 2) : undefined)"
