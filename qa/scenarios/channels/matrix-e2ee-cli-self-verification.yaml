title: Matrix E2EE CLI interactive self-verification establishes identity trust
scenario:
  id: matrix-e2ee-cli-self-verification
  surface: channels
  coverage:
    primary:
      - channels.access-and-identity
  objective: "Preserve the retired qa-matrix behavior: Matrix E2EE CLI interactive self-verification
    establishes identity trust."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-e2ee-cli-self-verification with the original
      Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix E2EE CLI interactive self-verification establishes identity trust
    timeoutMs: 180000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-cli-self-verification
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE CLI Self Verification Room
            requireMention: true
