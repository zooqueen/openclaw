title: Matrix approval target=both delivers channel and DM metadata once
scenario:
  id: matrix-approval-channel-target-both
  surface: channels
  coverage:
    primary:
      - channels.native-approval-prompts
  execution:
    profiles: { "matrix:all": 30, "matrix:transport": 29 }
    kind: flow
    channel: matrix
    timeoutMs: 90000
    retryCount: 0
    config:
      matrixConfigOverrides:
        approvalForwarding:
          exec: true
        dm:
          enabled: true
        execApprovals:
          enabled: true
          target: both
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - key: driver-dm
            kind: dm
            members:
              - driver
              - sut
            name: Matrix QA Driver/SUT DM

flow:
  module: ./live-transports/matrix/scenarios/scenario-runtime-approval.js
  call: runApprovalChannelTargetBothScenario
  args:
    - expr: "scenarioContext"
