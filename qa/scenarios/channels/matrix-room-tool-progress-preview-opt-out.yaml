title: Matrix streaming can opt out of preview tool progress
scenario:
  id: matrix-room-tool-progress-preview-opt-out
  surface: channels
  coverage:
    primary:
      - channels.streaming
  objective: "Preserve the retired qa-matrix behavior: Matrix streaming can opt out of preview tool progress."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-room-tool-progress-preview-opt-out with the
      original Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix streaming can opt out of preview tool progress
    timeoutMs: 60000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        streaming:
          mode: quiet
          preview:
            toolProgress: false
        toolProfile: coding
