title: Matrix reactions do not trigger a fresh bot reply
scenario:
  id: matrix-reaction-not-a-reply
  surface: channels
  coverage:
    primary:
      - channels.message-actions
  objective: "Preserve the retired qa-matrix behavior: Matrix reactions do not trigger a fresh bot reply."
  successCriteria:
    - The QA Lab Matrix adapter completes matrix-reaction-not-a-reply with the original
      Matrix-specific assertions.
  docsRefs:
    - docs/channels/matrix.md
    - docs/concepts/qa-e2e-automation.md
  codeRefs:
    - extensions/qa-lab/src/live-transports/matrix/scenarios/scenario-runtime.ts
  execution:
    kind: transport
    channel: matrix
    summary: Matrix reactions do not trigger a fresh bot reply
    timeoutMs: 8000
    retryCount: 0
    config:
      requiredChannelDriver: live
