title: Matrix E2EE stale own devices can be removed without deleting the current device
scenario:
  id: matrix-e2ee-stale-device-hygiene
  surface: channels
  coverage:
    primary:
      - channels.access-and-identity
  execution:
    kind: flow
    channel: matrix
    timeoutMs: 90000
    retryCount: 0
    config:
      requiredChannelDriver: live
      matrixConfigOverrides:
        encryption: true
        startupVerification: off
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: true
            key: e2ee-stale-device-hygiene
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA E2EE Stale Device Hygiene Room
            requireMention: true

flow:
  module: ./live-transports/matrix/scenarios/scenario-runtime-e2ee.js
  call: runMatrixQaE2eeStaleDeviceHygieneScenario
  args:
    - expr: "scenarioContext"
