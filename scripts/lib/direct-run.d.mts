export function isDirectRunPath(
  directPath: string | undefined,
  modulePath: string | undefined,
  platform?: NodeJS.Platform,
): boolean;
export function isDirectRunUrl(
  directPath: string | undefined,
  moduleUrl: string,
  platform?: NodeJS.Platform,
): boolean;
