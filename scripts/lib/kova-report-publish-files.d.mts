#!/usr/bin/env node
export function copyBundleMetadata({
  bundleDir,
  destinationDir,
}: {
  bundleDir: unknown;
  destinationDir: unknown;
}): string[];
export function assertPublishedFileSizeLimit({
  publishRoot,
  maxFileBytes,
}: {
  publishRoot: unknown;
  maxFileBytes: unknown;
}): number;
