/**
 * Public target-registry barrel for CLI/configure/audit callers. Query helpers
 * own discovery and resolution against the generated registry data.
 */
export * from "./target-registry-query.js";
