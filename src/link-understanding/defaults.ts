/** Default per-link fetch and CLI timeout when link tool config omits one. */
export const DEFAULT_LINK_TIMEOUT_SECONDS = 30;

/** Default cap for links extracted from one inbound message. */
export const DEFAULT_MAX_LINKS = 3;
